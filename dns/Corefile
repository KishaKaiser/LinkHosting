# Auto-generated by LinkHosting — do not edit manually
# CoreDNS configuration for internal DNS service
#
# DOMAIN_SUFFIX is supplied as an environment variable from docker-compose.yml.
# Queries for *.{DOMAIN_SUFFIX} are answered from the managed hosts file;
# all other queries are forwarded to upstream public resolvers.

{$DOMAIN_SUFFIX} {
    hosts /data/dns/hosts {
        # Re-read the hosts file every 5 s so new site records are picked up
        # automatically without requiring a manual reload.
        reload 5s
        ttl 30
    }
    # NXDOMAIN for any .{DOMAIN_SUFFIX} name not in our hosts file.
    errors
    log
}

. {
    forward . 1.1.1.1 8.8.8.8 {
        prefer_udp
    }
    cache 30
    errors
}
